#include "main.h"
#include <string.h>
/**
 * _strlen - program to calculate length of a string
 *
 * @s: input strength
 *
 * Return: void
 */

int _strlen(char *s)
{
	int len;
	
	len = strlen(s);
	return (len);
}
