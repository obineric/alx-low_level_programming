#include "main.h"

/**
 * _strlen - program that returns length of string
 * @s: string input
 * Return: length in intergers
 */

int _strlen_recursion(char *s)
{
	if (s[i] == '\0')
	{
		return (0);
	}
	else
	{
		return 
